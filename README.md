# Modelo-State
El patrón State permite que un objeto cambie su comportamiento dinámicamente según su estado interno. En lugar de usar múltiples condicionales (if o switch), el objeto delega las acciones a clases específicas que representan cada estado. Así, el mismo método puede ejecutar comportamientos distintos dependiendo del estado actual.
